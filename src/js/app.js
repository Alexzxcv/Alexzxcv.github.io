"use strigt"

let options = {
    tupe: '',
    design: '',
    adaptability: ''
};

options.tupe = prompt("Введите один из вариантов типов сайта \nВизитка, Магазин, Форум");
// options.design = prompt ("Введите один из вариантов дизайнов сайта \nКлассический, Инфорамционный, Корпоративный");
// options.adaptability = prompt ("Адаптивность \nДа, Нет");

let createSum = (a) => {

    
    let sum = this.a;
    return sum;
}

let cc = createSum(tupe);


alert (`Сумма вашего заказа: \n${cc}`);